const quoteButton = document.querySelector('js-new-quote');
//this assigns a reference to the quote button to a NEW variable called newQuoteButton. Now we can use newQuoteButton forever in our js code


//Use a DOM event signal to monitor when a user clicks on the page

quoteButton.addEventListener('click', getQuote);

function getQuote()
{

}